name: Deploy to Production

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    env:
      NODE_VERSION: '20'
      FIREBASE_PROJECT: momentocake-admin

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

      - name: Run type check
        run: npm run type-check

      - name: Build application
        run: npm run build
        env:
          NODE_ENV: production
          NEXT_PUBLIC_ENVIRONMENT: prod
          NEXT_PUBLIC_FIREBASE_API_KEY_PROD: AIzaSyATduKk-upKyADYwGqVST_xsUWd6DqPEi0
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN_PROD: momentocake-admin.firebaseapp.com
          NEXT_PUBLIC_FIREBASE_PROJECT_ID_PROD: momentocake-admin
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET_PROD: momentocake-admin.firebasestorage.app
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID_PROD: 958033613399
          NEXT_PUBLIC_FIREBASE_APP_ID_PROD: 1:958033613399:web:b20b1fbf2004c780f44912

      - name: Setup Firebase CLI
        run: npm install -g firebase-tools

      - name: Authenticate to Firebase
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_KEY }}

      - name: Deploy to Firebase Hosting
        run: |
          firebase use ${{ env.FIREBASE_PROJECT }}
          firebase deploy --only hosting --non-interactive
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}

      - name: Comment deployment URL
        uses: actions/github-script@v7
        if: github.event_name == 'push'
        with:
          script: |
            const deployUrl = 'https://${{ env.FIREBASE_PROJECT }}.web.app'
            github.rest.repos.createCommitComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              commit_sha: context.sha,
              body: `🚀 Production deployment successful!\n\n📝 App URL: ${deployUrl}`
            })