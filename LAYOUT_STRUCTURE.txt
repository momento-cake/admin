================================================================================
CLIENT MODAL LAYOUT STRUCTURE - VISUAL GUIDE
================================================================================

RELATED PERSONS SECTION
────────────────────────────────────────────────────────────────────────────

┌──────────────────────────────────────────────────────────────────────────┐
│ Card Container (p-3 flex items-start justify-between gap-4)             │
│                                                                          │
│  ┌─────────────────────────────────────────────┐  ┌──────────────────┐  │
│  │ Content Div (flex-1 min-w-0)                │  │ Buttons Div      │  │
│  │                                             │  │ (flex gap-2      │  │
│  │  ┌──────────────────────────────────────┐   │  │  flex-shrink-0   │  │
│  │  │ João Silva  Filho(a)  Nasc: 01/01/90 │   │  │  mt-0)           │  │
│  │  └──────────────────────────────────────┘   │  │                  │  │
│  │                                             │  │  ┌────┐  ┌────┐  │  │
│  │  ┌──────────────────────────────────────┐   │  │  │ ✏️ │  │ 🗑️ │  │  │
│  │  │ joao@email.com  (11) 98765-4321      │   │  │  │Edit│  │Del │  │  │
│  │  └──────────────────────────────────────┘   │  │  └────┘  └────┘  │  │
│  │                                             │  │                  │  │
│  └─────────────────────────────────────────────┘  └──────────────────┘  │
│                                                                          │
│  ← LEFT SIDE (fills available space)      RIGHT SIDE (fixed width) →   │
└──────────────────────────────────────────────────────────────────────────┘


CSS Breakdown:
──────────────

Card Container:
  flex                → Creates flex container (horizontal by default)
  items-start         → Aligns items to top vertically
  justify-between     → Maximum space between content and buttons
  gap-4               → 1rem gap between children
  p-3                 → 0.75rem padding all sides

Content Div:
  flex-1              → Grows to fill available space (pushes buttons right)
  min-w-0             → Allows text truncation if needed

Buttons Div:
  flex                → Makes buttons horizontal
  gap-2               → 0.5rem gap between buttons
  flex-shrink-0       → Prevents buttons from shrinking
  mt-0                → Zero margin-top (stays aligned with content top)


SPECIAL DATES SECTION
────────────────────────────────────────────────────────────────────────────

┌──────────────────────────────────────────────────────────────────────────┐
│ Card Container (p-3 flex items-start justify-between gap-4)             │
│                                                                          │
│  ┌─────────────────────────────────────────────┐  ┌──────────────────┐  │
│  │ Content Wrapper (flex gap-3 flex-1 min-w-0) │  │ Buttons Div      │  │
│  │                                             │  │ (flex gap-2      │  │
│  │  ┌──┐  ┌──────────────────────────────┐     │  │  flex-shrink-0   │  │
│  │  │🎂│  │ Aniversário                  │     │  │  mt-0)           │  │
│  │  └──┘  │ 25 de dezembro de 2025       │     │  │                  │  │
│  │        │                              │     │  │  ┌────┐  ┌────┐  │  │
│  │        │ Aniversário → João Silva     │     │  │  │ ✏️ │  │ 🗑️ │  │  │
│  │        └──────────────────────────────┘     │  │  │Edit│  │Del │  │  │
│  │                                             │  │  └────┘  └────┘  │  │
│  └─────────────────────────────────────────────┘  └──────────────────┘  │
│                                                                          │
│  ← LEFT SIDE (icon + content fills space)    RIGHT SIDE (fixed) →      │
└──────────────────────────────────────────────────────────────────────────┘


CSS Breakdown:
──────────────

Card Container:
  flex                → Creates flex container
  items-start         → Aligns items to top
  justify-between     → Maximum space between content and buttons
  gap-4               → 1rem gap

Content Wrapper:
  flex                → Horizontal layout for icon + text
  gap-3               → 0.75rem gap between icon and text
  flex-1              → Grows to fill space
  min-w-0             → Allows text truncation

Icon Div:
  text-xl             → Large emoji/icon
  flex-shrink-0       → Icon doesn't shrink

Text Content Div:
  flex-1              → Fills remaining space
  min-w-0             → Allows truncation

Buttons Div:
  flex                → Horizontal button layout
  gap-2               → 0.5rem between buttons
  flex-shrink-0       → Buttons don't shrink
  mt-0                → Zero top margin (vertical alignment)


RESPONSIVE BEHAVIOR
────────────────────────────────────────────────────────────────────────────

Desktop (>= 768px):
┌────────────────────────────────────────────────────────────────┐
│  João Silva  Filho(a)                      [Edit]    [Delete]  │
│  joao@email.com  (11) 98765-4321                               │
└────────────────────────────────────────────────────────────────┘

Mobile (< 768px):
┌──────────────────────────────────┐
│  João Silva      [Edit] [Delete] │
│  Filho(a)                        │
│  joao@email.com                  │
│  (11) 98765-4321                 │
└──────────────────────────────────┘

Note: Buttons stay on RIGHT even on mobile due to flex-shrink-0
      Content wraps naturally with flex-wrap on smaller screens


KEY IMPROVEMENTS FROM PREVIOUS VERSION
────────────────────────────────────────────────────────────────────────────

BEFORE (incorrect):
┌────────────────────────────────┐
│  João Silva  Filho(a)          │
│  joao@email.com                │
│  (11) 98765-4321               │
│                                │
│  [Edit]    [Delete]   ← BELOW  │
└────────────────────────────────┘

AFTER (correct):
┌───────────────────────────────────────────┐
│  João Silva  Filho(a)    [Edit] [Delete]  │
│  joao@email.com                            │
│  (11) 98765-4321                           │
└───────────────────────────────────────────┘


CHANGES MADE:
1. Added `items-start` to Card → Aligns children to top
2. Added `justify-between` to Card → Pushes content LEFT, buttons RIGHT
3. Changed content to `flex-1` → Takes available space
4. Changed buttons to `flex-shrink-0` → Fixed width, stays RIGHT
5. Added `mt-0` to buttons → No top margin, aligns with content

================================================================================
